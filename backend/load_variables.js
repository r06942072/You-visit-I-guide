//load variable (for example in breadcrumb)
import wixWindow from 'wix-window';

export function load(page_id) {
    let language = wixWindow.multilingual.currentLanguage;
    if (language==)
    //let myTitle = $w("#myPage").title;
    return language;
    //console.log(myTitle);
}